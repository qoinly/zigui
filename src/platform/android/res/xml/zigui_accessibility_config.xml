<?xml version="1.0" encoding="utf-8"?>
<!-- zigui's default accessibility-service config. Capabilities the system grants:
     gesture injection (canPerformGestures) plus reading the foreground window
     content (canRetrieveWindowContent + flagRetrieveInteractiveWindows). An app
     that needs a tighter set ships its own xml and points the manifest at it. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeAllMask"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagRetrieveInteractiveWindows"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:notificationTimeout="100" />
